<?xml version="1.0" encoding="utf-8" ?>

<!-- Add Food/Drink Record Page -->
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FoodDrinkApp.AddItemPage"
             Title="Add Record"
             BackgroundColor="{AppThemeBinding Light=#F8F4FF, Dark=#1E1A24}">

    <!-- Allows the page content to scroll on smaller screens -->
    <ScrollView>

        <!-- Main vertical layout container -->
        <VerticalStackLayout Padding="20"
                             Spacing="20">

            <!-- Top banner section -->
            <Grid HeightRequest="180">

                <Border StrokeThickness="0">

                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="30"/>
                    </Border.StrokeShape>

                    <!-- Gradient background for banner -->
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0"
                                             EndPoint="1,1">

                            <GradientStop Color="#D8C3FF"
                                          Offset="0"/>

                            <GradientStop Color="#E9DEFF"
                                          Offset="0.55"/>

                            <GradientStop Color="#FFE9A8"
                                          Offset="1"/>

                        </LinearGradientBrush>
                    </Border.Background>

                </Border>

                <!-- Banner text content -->
                <VerticalStackLayout Padding="24"
                                     VerticalOptions="Center">

                    <Label Text="Create New Entry"
                           FontSize="32"
                           FontAttributes="Bold"
                           TextColor="#4B3B5A"/>

                    <Label Text="Store meals, beverages and nutrition information in one place."
                           FontSize="15"
                           TextColor="#5F5172"/>

                </VerticalStackLayout>

            </Grid>

            <!-- Section title for meal information -->
            <Label Text="MEAL INFORMATION"
                   FontSize="14"
                   FontAttributes="Bold"
                   TextColor="#8B6FB3"/>

            <!-- Card containing basic meal details -->
            <Border BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#31283C}"
                    Stroke="{AppThemeBinding Light=#D8C5FF, Dark=#4D405D}"
                    Padding="18">

                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="24"/>
                </Border.StrokeShape>

                <VerticalStackLayout Spacing="16">

                    <!-- Food or drink name input -->
                    <Entry x:Name="NameEntry"
                           Placeholder="Meal or drink name"
                           BackgroundColor="{AppThemeBinding Light=#F7F2FF, Dark=#3A3047}"
                           TextColor="{AppThemeBinding Light=#4B3B5A, Dark=White}"
                           PlaceholderColor="#A494BF"/>

                    <!-- Category selection dropdown -->
                    <Picker x:Name="CategoryPicker"
                            Title="Choose category"
                            BackgroundColor="{AppThemeBinding Light=#F7F2FF, Dark=#3A3047}"
                            TextColor="{AppThemeBinding Light=#4B3B5A, Dark=White}">

                        <Picker.Items>
                            <x:String>Breakfast</x:String>
                            <x:String>Lunch</x:String>
                            <x:String>Dinner</x:String>
                            <x:String>Snack</x:String>
                            <x:String>Beverage</x:String>
                        </Picker.Items>

                    </Picker>

                    <!-- Detailed description input -->
                    <Editor x:Name="DescriptionEditor"
                            HeightRequest="120"
                            AutoSize="TextChanges"
                            BackgroundColor="{AppThemeBinding Light=#F7F2FF, Dark=#3A3047}"
                            TextColor="{AppThemeBinding Light=#4B3B5A, Dark=White}"
                            Placeholder="Describe ingredients, flavour or preparation"/>

                </VerticalStackLayout>

            </Border>

            <!-- Nutrition information panel -->
            <Border BackgroundColor="{AppThemeBinding Light=#FFF8DA, Dark=#4B3B22}"
                    StrokeThickness="0"
                    Padding="18">

                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="24"/>
                </Border.StrokeShape>

                <VerticalStackLayout Spacing="16">

                    <!-- Nutrition section heading -->
                    <Label Text="NUTRITION VALUES"
                           FontAttributes="Bold"
                           FontSize="15"
                           TextColor="{AppThemeBinding Light=#9A6800, Dark=#FFD978}"/>

                    <!-- Nutrition data input grid -->
                    <Grid ColumnDefinitions="*,*"
                          RowDefinitions="Auto,Auto"
                          ColumnSpacing="12"
                          RowSpacing="12">

                        <!-- Calories input -->
                        <Entry x:Name="CaloriesEntry"
                               Placeholder="Calories"
                               Grid.Row="0"
                               Grid.Column="0"
                               Keyboard="Numeric"
                               BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#3A3047}"
                               TextColor="{AppThemeBinding Light=#4B3B5A, Dark=White}"/>

                        <!-- Protein input -->
                        <Entry x:Name="ProteinEntry"
                               Placeholder="Protein g"
                               Grid.Row="0"
                               Grid.Column="1"
                               Keyboard="Numeric"
                               BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#3A3047}"
                               TextColor="{AppThemeBinding Light=#4B3B5A, Dark=White}"/>

                        <!-- Carbohydrates input -->
                        <Entry x:Name="CarbsEntry"
                               Placeholder="Carbohydrates g"
                               Grid.Row="1"
                               Grid.Column="0"
                               Keyboard="Numeric"
                               BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#3A3047}"
                               TextColor="{AppThemeBinding Light=#4B3B5A, Dark=White}"/>

                        <!-- Fat input -->
                        <Entry x:Name="FatEntry"
                               Placeholder="Fat g"
                               Grid.Row="1"
                               Grid.Column="1"
                               Keyboard="Numeric"
                               BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#3A3047}"
                               TextColor="{AppThemeBinding Light=#4B3B5A, Dark=White}"/>

                    </Grid>

                </VerticalStackLayout>

            </Border>

            <!-- Dietary and allergy information section -->
            <Border BackgroundColor="{AppThemeBinding Light=#F7F2FF, Dark=#31283C}"
                    Stroke="{AppThemeBinding Light=#D8C5FF, Dark=#4D405D}"
                    Padding="18">

                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="24"/>
                </Border.StrokeShape>

                <VerticalStackLayout Spacing="10">

                    <!-- Section heading -->
                    <Label Text="DIETARY NOTES"
                           FontAttributes="Bold"
                           TextColor="{AppThemeBinding Light=#8B6FB3, Dark=#D6BFFF}"/>

                    <!-- Allergy and dietary restriction input -->
                    <Entry x:Name="AllergyEntry"
                           Placeholder="Allergy information or dietary restrictions"
                           BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#3A3047}"
                           TextColor="{AppThemeBinding Light=#4B3B5A, Dark=White}"
                           PlaceholderColor="#A494BF"/>

                </VerticalStackLayout>

            </Border>

            <!-- Validation message panel -->
            <Border x:Name="ValidationPanel"
                    IsVisible="False"
                    Padding="14"
                    BackgroundColor="{AppThemeBinding Light=#FFF4D6, Dark=#5A4722}">

                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="18"/>
                </Border.StrokeShape>

                <!-- Displays validation error messages -->
                <Label x:Name="ValidationLabel"
                       FontAttributes="Bold"
                       TextColor="{AppThemeBinding Light=#A06B00, Dark=#FFD978}"/>

            </Border>

            <!-- Save button for submitting the record -->
            <Button Text="Save Entry"
                    HeightRequest="62"
                    CornerRadius="28"
                    FontSize="18"
                    FontAttributes="Bold"
                    Clicked="OnSaveClicked"
                    BackgroundColor="{AppThemeBinding Light=#CDB4FF, Dark=#8B6CC7}"
                    TextColor="{AppThemeBinding Light=#4B3B5A, Dark=White}"
                    Margin="0,10,0,20"/>

        </VerticalStackLayout>

    </ScrollView>

</ContentPage>